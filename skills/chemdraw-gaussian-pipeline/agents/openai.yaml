interface:
  display_name: "ChemDraw to Gaussian"
  short_description: "Create checked Cartesian inputs and GaussView previews"
  default_prompt: "Use $chemdraw-gaussian-pipeline to convert this ChemDraw file into a checked Cartesian Gaussian input and open it in GaussView without running a calculation."
