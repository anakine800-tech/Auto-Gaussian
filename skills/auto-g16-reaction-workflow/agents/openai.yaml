interface:
  display_name: "Auto-G16 Reaction Workflow"
  short_description: "Build offline reaction plans and bounded analyses"
  default_prompt: "Use $auto-g16-reaction-workflow to build and validate reviewed offline reaction artifacts, a non-executable calculation plan, a read-only blocker-aware study index, or separate experimental bounded-analysis artifacts without granting live authority."
