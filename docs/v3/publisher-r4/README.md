# Auto-G16 R4 publisher offline implementation dossier

## Frozen authority

Task `V31-PUBLISHER-R4-OFFLINE-IMPLEMENTATION-01`, feature development, v3,
OWNER-GUIDED, L3 security/scheduler boundary. Owner accepted the independently
reviewed exact four-file package on 2026-09-15 (reply: **通过**).
[Provenance](provenance.json) retains original SHA-256/length and derived file
identities. Source packet SHA-256:
`12ccdebb10b4f1770910e7a0dafe7c74fee99332a4e1ffa7b2cf68c2b8c03c06`.

Normative order: [R4](publisher-pilot-r4-dependency-delta.md) overrides
[R3 interface](publisher-pilot-candidate-r3-interface-delta.md) and
[R3 schema](publisher-pilot-r3-schema-and-versions.md), which override
[R2](publisher-pilot-candidate-r2.md). Their original candidate-status labels
are historical, superseded only by this exact offline implementation acceptance.
The external independent R4 final review reported LIMITED TECHNICAL PASS,
with no remaining P0/P1/P2 in its design-only scope. It did not review code.

## Isolation and scope

Base `3116d1f1919eff164ef3593171d0ed7f42c778b5`, tree
`32fe6df47c947988311497fbd4b2de75d97c1a34`; unique linked worktree on
`codex/v31-publisher-r4-offline-integration`. Initial require-clean preflight:
PASS, seven checks, zero warnings/blockers. No BUS lane or Control Issue.

Allowed: private completion grammar/wrapper, Execution program and program_runtime,
Transport _program_rtwin, finite scripts/run_v31_publisher_pilot.py, corresponding
v31/transport tests, exact selector route, five authority references and this
dossier. Public Core/Approval/Result, seven Transport operations, store DDL,
four-key runtime qualification and _driver.py remain fixed.

## Validation and gates

Use new/affected focused tests, author findings-first review, exact candidate
freeze and selector base/head evidence. Parent task arranges independent review.
Selector self-protection escalation is reported without altering gates or running
full discovery. New production source requires its own Linux evidence; historical
FC06 and prototype totals do not qualify it. Actual locator, installed physical
identities and host facts are NOT_ACQUIRED. Production remains unavailable.

No push, PR, merge, deployment, network/SSH/PBS, real scientific program execution, cancellation,
cleanup or scientific acceptance. Local checkpoint commits retain normal hooks
and staged sensitive scans. Worktree/branch retained pending later disposition.

Bounded inert offline child processes are permitted only inside the reviewed
fixture's resource and owned-process limits. They do not qualify a target.

## Implemented private evidence encoding

The unavailable installation is a source-owned `_FixedPublisherInstallation`
with exact basis/Q/original-file paths, parent chains, file device/inode,
SHA-256/size and reviewed source commit/tree. No environment, CLI, caller path,
current-profile path, Q field or file search can supply its locator. Each pinned
file is read-only, no-follow and retained/rechecked at effect boundaries.
The independently fixed Controller `_FixedPilotRun` binds existing three store
handles and physical paths, three approval IDs, profile/snapshot, exact input
and scheduler bytes, six actual code-file identities and reviewed scope file.
Neither installed binding currently has a production value (both are `None`).

The canonical reviewed scope file (maximum 64 KiB) contains exactly
`schema=v31-publisher-reviewed-scope/1`, `owner_exact_q`, `live_gate`.
Each role contains exactly `raw={sha256,size_bytes}` and `scope`.
Owner scope contains Q payload/file hashes. Live Gate scope adds profile ID,
snapshot ID, Attempt ID and pilot_window. Original bytes and this interpretation
are both independently pinned at installation; code never infers acceptance
from prose, a keyword, an `approved` flag or the file's existence.

The canonical raw probe index (maximum 1 MiB) contains exactly
`schema=v31-publisher-probe-evidence-index/1` and ordered `entries`:

- scheduler-scope: role, evidence;
- controller-probe: role, case_id=P08, evidence;
- for each ordered host: host-identity (role, host_key, evidence), four location
  entries (role, host_key, location_role, evidence), seven host-probe entries
  (role, host_key, case_id, evidence).

Each evidence is the Q's exact Digest; indexed raw files are independently pinned,
nonempty, at most 16 MiB each. These are finite private read bounds, not target
probe permission. The index and reviewed scope add no basis or OC fields and
are not reflected back into Q/profile/snapshot. P08 evidence must originate
from prior independent synthetic fixtures, never the final Q or its installation.
Human review still establishes the actual probe provenance and acceptance.

## Source and invocation compatibility

Historical source remains 14181 bytes, SHA-256
`58167de5436ec2d5ae9ef89dde458f386cdc2860f4f00a41c81c0a390f782333`.
Default material/1 selects scheduler/2 + B/2. Explicit material/2 selects
scheduler/3 + B/3 + the independent built-in publisher source. Current Q cannot
upgrade historical artifacts. All dispatch/replay uses the persistent tuple.

Only scheduler/3 sends canonical base64 configuration over a fixed single-quoted
heredoc; configuration cannot become shell expansion or a Linux oversized argv.
The fixed source argument is capped at 64 KiB and stdin base64 at 8 MiB, preserving
accepted Q (1 MiB) and material/2 (5 MiB) limits. Child stdin remains DEVNULL.
Actual-host checks run before workspace launch setup, immediately before child,
and after pending fsync/close/readback immediately before atomic link. The old
wrapper, receipt schema, seven operations and four runtime keys remain unchanged.

## Controller and completion ordering

First submission: fixed code/stores/deployment -> current three records and OC
assert -> pinned original/scope checks -> existing validate_effect_authority ->
mechanical port -> current records/evidence and validator again -> execute_once.
Only existing Execution creates a WINNER and invokes its one-use continuation.
Existing repeated claim reads can return REPLAY; they are not additional WINNERs.
After a claim, drift stops further effects and preserves the consumed Attempt.

The separately callable bounded collector rechecks the same installed pilot and
current approvals, without using the PLANNED-only validator after submission.
Terminal states use local replay; unfinished submitted states allow one bounded
absence epoch. Transport rechecks mechanical qualification on each operation;
Execution rechecks the real driver before bundle/assessment persistence, state
promotion and proof return. Raw history and valid legacy synthetic completion
remain independent of real Q/window checks. There is no polling or restart loop.

## Author review and remaining handoff

Author review found and repaired oversized-config argv handling, the procfs
self-link/no-follow conflict (mountinfo uses this process's numeric PID), the
publication guard placement, pre-Project queue mismatch, and the completion
promotion checkpoint. Target qualification remains NOT_ACQUIRED. Real production
factory/Controller/driver/wire construction and receipt completion are tested
with the process owner replaced by an inert peer; that is product composition
evidence, not actual Linux process or target evidence. No original FC06 or external
prototype test result is reassigned to this source.

The parent task owns independent review, new-source Linux validation and any
required one-time final selector escalation. No full discovery or remote CI was
run during authoring. Exact final source/file hashes and raw local test logs are
retained in the external candidate handoff. Worktree and branch are retained.
