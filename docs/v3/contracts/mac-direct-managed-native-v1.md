# Auto-G16 MAC-DIRECT-MANAGED-NATIVE-V1

## Authorization and identity

Owner-guided v3 feature development, L3 security boundary, not BUS. The
parent task `01a0d199-6213-7502-9398-6d7e9b4d854d` authorized implementation,
offline validation, author review and an uncommitted frozen handoff only.
Base/HEAD is `b9a5fb1ce24bc708e60ddb4424aae493c440c171`.
App worktree is `<codex-home>/worktrees/6577/<repository>`, branch
`codex/mac-direct-managed-native-v1`. Initial tree was clean. Startup preflight
passed all seven checks, exit 0, with no modifiers or live flags.

No accounts, keys, services, installation, permission changes, native
qualification experiments, networking, SSH/PBS/Gaussian, commit/push/PR/merge,
deployment, cleanup or retry. First validation failure stops implementation
and validation; retain evidence and hand off. No subagents; parent owns
independent review. Identity/scope/contract drift stops immediately.

## Exact file boundary

The 18 source files in external manifest
`mac-direct-schema-wrapper-resume-20260925T134843Z/candidate-files.json`
(SHA256 `a5c88057add4e3b3cd4322a630e60e4ec5f7149e3c3603d8a402a70b98678e67`)
were all rehashed successfully at source HEAD
`c1e39c409370f0dd639b0e2739e75ca6e04a51fe`. Port the 14 additions and the
two test corrections. Merge only the added component/routing entries in the
two configs; preserve main's Gaussian routes. The historical task document is
retained as history and does not grant this task authority.

Additional exact new files:

- `auto_g16/_managed_native/__init__.py`
- `auto_g16/_managed_native/installation.py`
- `auto_g16/_managed_native/ipc.py`
- `auto_g16/_managed_native/lifecycle.py`
- `auto_g16/_managed_native/supervisor.py`
- `auto_g16/_managed_native/darwin.py`
- `auto_g16/_managed_native/native.c`
- `auto_g16/_managed_native/service.py`
- `tests/v3/managed_native/__init__.py`
- `tests/v3/managed_native/test_boundaries.py`
- `tests/v3/managed_native/test_supervisor.py`
- this task contract.

Additional existing files permitted: `OWNER_DECISIONS.md`,
`docs/v3/boundary-spec.md`, `docs/v3/acceptance.md`, and the two configs above.
Only the following five Transport files are needed: `_direct.py` for the
closed parser/authority/builder and attestor; `_driver.py` for mutually
exclusive profile resolution and normal launch dispatch; `_recovery_process.py`
to prevent a second Direct process owner; `_program_rtwin.py` for source-owned
command preparation; `_bridge.py` for the shared fixed source renderer.
All five are under `auto_g16/transport/`. No directory-wide Transport allowance.

Old Direct dependency HEAD is `34b8f035552ee97ac969c58bb7ed36f7b55f87ef`.
All 15 files match repaired manifest SHA256
`1be90235b3c27f9a770c5ed7acf956e14e4d2560482a513249b15f5260aba81c`.
Independent `mac-direct-l3-final-20260924T085853Z/REVIEW.md` binds that manifest
and closes F1/F2/R1, 0/0/0/0; it is offline reuse evidence, not native evidence.
PORT the exact `_direct.py`; EXTRACT the four necessary source hunks into
current main, preserving current Gaussian source selection and artifact code.
WRAP Direct dispatch with the closed managed entry. Do not port old whole files
over main, old publication permission, or old native-readonly qualification.

## Boundary

R5 overrides its named R4 clauses; R4 A/B/C inherits unchanged R3/R2.
The parent-supplied independently reviewed N1–N7 replace Popen supervision:
one native owner and no second reaper; C -> original owner guard -> M spans
suspended posix_spawn, PID/handle ownership, continuous EVFILT_PROC registration
and checked EV_RECEIPT, SIGCONT and registration. Observe cumulative FORK/EXEC/
faults before accepting matching EXIT, real non-reaping waitid and two EOFs;
only then uniquely reap nonblocking. ECHILD/EPERM/no state never proves exit.
Any uncertainty poisons permanently; no descendant discovery/signaling or
retry. At most five extra user-space seconds retain unresolved handles. A
blocked kernel call cannot be represented as successful STOP.

Absolute `/usr/bin/ssh`, source-owned argv/environment/cwd, SETSID,
CLOEXEC_DEFAULT and managed signal disposition remain closed. Native completed
means process observation only. Core owns intent/START/receipt observations;
Transport owns physical authority. No public Core/Approval/Attempt/store
schema or selector algorithm changes. UNKNOWN never retries.

Only fresh installation, one lifetime, and original restricted xTB material.
No CREST or recovery/collection continuation UI. Codex/explicit human relay
source is the accepted assumption; it gives desktop UID no execution-domain
write authority. Private keys remain metadata-only; every readable dependency
must exclude protected private inode before reading. All path components use
no-follow traversal. No ProxyJump/ProxyCommand injection, password, TOFU,
global configuration or fallback. Existing Option-1 behavior is preserved.

Installation paths are design templates only: `/Library/AutoG16/installations/`,
`/private/var/db/auto-g16/`, `/Library/AutoG16/ipc/` with an immutable installation
ID and future system LaunchDaemon. Exact UID/ACL/interpreter/dependencies and
native library build are unqualified. No mutable current link or /var alias.
Production entry always rejects before filesystem mutation or native loading;
no client flag, environment, fake or manifest can activate it.

## Validation and acceptance

Offline adapters test closed data, forbidden fields, peer observation errors,
attachments/framing/EOF, G/C/M ordering, marker durability call order, permanent
veto and N1–N7 ordering/fault accumulation. Fakes prove only Python call/state
order. Native source is retained for review but must not be compiled, loaded,
spawned or experimentally qualified in this task. Native process, cross-UID,
service, fsync/crash, ACL and fork experiments are NOT RUN.

Then run necessary affected compatibility for original managed material/review,
current Gaussian approval construction, `_program_artifacts.py`, split-root
recovery and Option-1; explicit-path selector preview only (no authoritative
commit selection for an uncommitted candidate). Config changes must preserve
selector self-protection. No old candidate full rerun. Finish with documentation,
static CI/Python checks and exact file hashes/commands/environment/exit and
child cleanup receipts. No remote CI assertion. Production CLOSED / native
UNQUALIFIED remains mandatory, including after all offline checks pass.

## F2 bounded offline composition repair

Owner continuation explicitly authorizes the existing branch to complete the
private service/original-owner composition, focused and necessary affected
offline checks, then an uncommitted freeze for independent review. First
validation failure stops; no full run, installation, networking or publication.
Production entry and concrete Darwin loading remain closed. No native source
compilation, process or filesystem qualification is authorized.

The additional test carriers are `tests/v3/managed_native/test_service.py` and
`tests/v3/managed_native/test_durable_veto.py`; the existing native selector
route includes both. Private service construction accepts only original
managed object references, never serialized client dependencies. C encloses
the original execute_once claim and receipt writes. Direct production-driver
authority requires that private context; each lower launch rechecks the same
Transport completion guard and post-claim Review scope before and under M.
The original source/publisher, request reconstruction and dual-store checks
remain in force. No second ledger or retry is introduced.

Missing Project/snapshot bindings still reject. Recovery/collection
continuations remain disabled. The consumer response follows R2 section 5;
local status reads original Core intent and recovery observations. Material
and review handlers reuse their existing exact codecs and owners. Reply loss
does not repeat execution. Lifecycle veto establishment cannot be retried on
the same object after any partial failure; it never removes the marker.

Offline service tests replace only the final process dispatch and Darwin
adapter with inert peers; real Core/Approval/Transport owners and production
driver logic remain. This is not qualified execution, and does not establish
endpoint installation, cross-UID protection, actual durability or C ABI proof.

## F2-R1/R2 invocation handoff and fixture repair

The explicit follow-up Owner gate authorizes only the two reviewed defects,
focused/affected checks, freeze and independent review on the same branch.
First failure still stops; production and all native/installation gates stay
closed. Changes are confined to the service, original Program driver, its
managed service tests and this contract clarification.

The original driver consumes `_active` at its unchanged point. Only after all
original predecessor checks and wire construction does it bind one private
Direct handoff to the exact driver/snapshot/invocation and request bytes,
process and thread. Low-level admission consumes that handoff once, including
on rejection; checks under M retain the original completion guard. A second
use, changed request, foreign invocation, thread/process or expired context
rejects. The finally path closes and removes the handoff without restoring
`_active`. No new durable authority or automatic retry exists.

The fixture uses the original production-generation provisioning journal,
Project service and snapshot factory. A test-only implementation of the
existing Project semantic interface supplies inert observations for the exact
Direct profile. It does not qualify the real Project attestor, change product
generation/type checks, install anything or contact a server. The real Program
driver, source/publisher checks, Approval/Core/Transport and supervisor remain
in the tested path; only remote/native mechanics are inert substitutes.
