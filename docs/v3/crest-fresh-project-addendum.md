# Auto-G16 CREST fresh Project delta

The parent coordination task accepted this narrower native path under the
Owner's bounded delegation on 2026-09-16. Independent review remains required
before the implementation or live candidate is accepted.

This supersedes only the contract's reuse of the existing product Project.
ProjectPhysicalBinding includes the resolved profile identity, and the current
native provisioning API does not rebind an existing Project to the new CREST
qualification/profile. Do not inject a binding or alter the old journal.

Create one new Project through the ordinary native fresh provisioning path,
under the same reviewed root and exact target identity. The planned directory
is `/home/user100/SDL/v31-crest-pilot-20260916`. First freeze and independently
review the exact native Project ID, profile, target/root/path and one fresh
provisioning operation before that directory creation. After native provisioning
returns the Project physical binding, freeze and independently review the exact
Attempt/snapshot, Q, handoff and submission packet before Attempt mutation or
qsub. Existing-path observations fail closed. This document does not approve an
as-yet-unformed live packet.

The receipt handoff retains the original Project binding and native source
proof. A separate immutable private fixed submission binds the entire new
Project physical binding, exact snapshot, CalculationPlan ID and intent hash.
The intent includes the handoff ID and payload digest. Immediately before the
sole claim, the production CREST adapter-3 port revalidates the fixed seed/spec,
source proof, handoff, plan and both Project bindings. Only an exact inert
fixture executable plus synthetic runtime qualification is exempt. Missing or
drifted authority causes zero claim and zero remote effect.

Source and destination Project IDs and directories must be distinct; target
identity and reviewed root must match. The source remains the original xTB
Attempt/job 693.master and native Result/capture, wholly read-only. No old
Project, journal or database is rewritten. This adds no generic adoption,
profile-rebinding API, Core schema or Transport operation.
