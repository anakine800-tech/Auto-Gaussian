# Auto-G16 v2.7 existing submitted-job lineage owner

Status: offline, synthetic/fake-validated W5 to W6 read-capability foundation;
the fixed fetch-successor handoff and offline exact-five reader are present.
Production query/fetch, cancellation, advanced inspection, live operation and
scientific acceptance remain blocked.

## Sole owner and legal starting point

`direct_existing_job_lineage` is the sole backend-owned observer for an
already submitted direct job. It never calls or widens the W1
fresh-project-mutation owner. Its only legal evidence inputs are the exact
canonical W5 portable receipt bytes and the exact reviewed W5 artifact byte
bundle. A receipt dict, job-id string, caller-selected root, reconstructed
binding, or portable projection is not authority.

Production issuance is restricted to the fixed server-local Python process
running with `-I -S`, cwd `/`, and the fixed `LANG=C`, `LC_ALL=C` environment.
The durable-state root remains the backend-owned W4B constant. Tests use only
the private fake factory and temporary POSIX directories.

This Python boundary does not claim to withstand arbitrary same-process
closure reflection or native-memory takeover. The fixed clean-exec process
must therefore exclude unreviewed imports, plugins and caller code; a successor
must preserve that process boundary rather than treating the portable
projection or test factory as authority.

## Read-only observation and currentness

The owner revalidates exact W1 profile-policy, stable-evidence, profile and
authorization bytes; W5 transport profile, SSH policy evidence, immutable
input and reviewed PBS bytes; and the canonical W5 receipt. It then opens the
profile-owned allowed root component by component with `O_NOFOLLOW`, opens the
existing project relative to the retained root descriptor, and opens
`submission-receipt.json` relative to the retained project descriptor. The
remote receipt must be a bounded, owner-only, single-link regular file whose
bytes exactly equal the reviewed W5 receipt bytes.

The same owner opens the fixed W2 state root component by component, takes the
read lock, and revalidates the exact manifest, started event and sole terminal
event. Only `started -> completed` is eligible. Started-only or `unknown` is
reconciliation-only. Unknown entries, duplicate terminal material, corrupt
hash chains, receipt/W2 splices, or drift in journal, binding, attempt,
project, input, authorization, resources, job ID, qsub-call count, invocation,
outcome, result or receipt hashes fail closed.

The issued capability retains the complete root and state component chains,
project, remote receipt, journal, lock, manifest, started and terminal FDs.
Every assertion replays FD and named-entry identity plus exact bytes. Symlink,
hard-link, root escape, path replacement, project replacement, receipt
replacement, source/module rebinding, closed/reused descriptor, or identity
drift invalidates the capability.

## Capability and blocked successors

Only the owner-private registry can issue
`DirectSubmittedJobReadCapability`. It is process-local, nonconstructible,
noncopyable, nonserializable, fork-revoked and single-use. Consumption yields
one descriptor-retaining `DirectSubmittedJobReadLease`. The lease has no
generic query, fetch, path, FD or callback port. Its sole additional owner
entry can terminalize it directly into the canonical
`direct_fetch_acquisition` successor without returning descriptors to the
caller. A failed successor closes the entire retained set.

The portable lineage projection binds the reviewed artifact hashes, W2
journal and binding hashes, attempt/project/input/authorization, exact job ID,
single qsub invocation/outcome/result/receipt hashes, and current project,
receipt and journal descriptor identities. It always states
`authorizes_effect=false`, `portable_projection_authorizes_read=false`, and
`scientific_acceptance=false`.

No code in this slice performs network access, SSH, qstat, PBS, Gaussian,
qsub, qdel, fetch, materialization, retry, cancellation, cleanup, deletion,
deployment or live work. The separately reviewed Q1 successor consumes the
exact live lease for a production-shaped, offline-validated qstat query without
reconstructing it from the projection. The offline F1 successor consumes the
exact live lease for the bounded terminal-minimum fetch stream. Production
composition of those successors, live query, live fetch and every effect
remain blocked and unauthorized.
