# Auto-G16 v2.6 PR4J legacy phase/effect owner extraction

Status: offline mechanical prerequisite only; no protected consumer or adapter

Task class: maintenance-compatible feature refactor / L3 execution boundary

Frozen baseline: commit `fc7b59dc6c280db6cdba435ae7e11f27cf30dd19`,
tree `572067d75ab80f17b962ebc77579401340b22730`, parent
`ba9fb6f523edbc23e94cd20a1fc5d4d085567cec`

## Baseline phase map

Line references identify the frozen baseline before PR4J. They distinguish the
legacy transaction policy from the fixed external effect sequence.

| Baseline lines | Phase | Existing owner and boundary |
| --- | --- | --- |
| 4903–5145 | preparation and materialization | Validate the plan, capture and parse the immutable input, replay batch/resource/input/live approvals, stage once, publish initial job state, verify staged bytes, and return before reservation for dry-run |
| 5147–5188 | exact reservation and state publication prerequisite | Replay live approval after staging and reserve the exact attempt under the existing resource owner; no runner call occurs before the reservation |
| 5189–5288 | post-reservation local materialization | Publish intent and approval consumption, rewrite checksums, calculate fixed timeouts, publish `submission_uncertain`, and verify every upload byte; on failure, use the legacy ledger/job reconciliation policy |
| 5290–5345 | fixed pre-qsub effects with one legacy state update | Invoke five runner steps in a fixed order; after the hash observation, parse it and publish the existing hash evidence before the final two transfer steps |
| 5346–5368 | pre-qsub transport failure policy | Reconcile the exact attempt to not-submitted only when local control flow proves qsub was not invoked; otherwise retain uncertainty on reconciliation failure |
| 5370–5395 | resource replay policy | Reload and compare resource policy, gate and scheduler artifacts and freshness; no raw effect belongs to this phase |
| 5397–5439 | live-approval replay policy | Revalidate the exact approval immediately before qsub; reconcile only through the existing legacy policy |
| 5441 | qsub state publication | Publish `qsub_invocation_started=True` while retaining `submission_uncertain` before the qsub runner call |
| 5442–5470 | qsub raw effect and outcome classification | Build the fixed script, invoke the runner once with `check=False`, classify unique/unknown output, and forbid retry on uncertainty |
| 5471–5514 | receipt and final reconciliation | Publish the immutable local receipt, reconcile the ledger to submitted, update final job state and print the compatible success JSON |

Dry-run remains entirely in preparation/materialization and executes zero
runner calls.

## Fixed raw-effect sequence

The private raw-effect owner accepts only one transaction-owner-issued frozen
plan. It contains exact derived paths, immutable file bindings, fixed
connection fields, finite timeouts, attempt identity and input hash. It accepts
no mapping, keyword expansion surface, callback, runner, command, backend
selector, runtime override, retry, cancel or cleanup authority.

| Order | Baseline call | Exact input | Timeout and check | Observation and failure boundary |
| --- | --- | --- | --- | --- |
| 1 | Windows directory claim | `ssh_base` plus the fixed encoded no-clobber PowerShell script | default finite timeout; `check=True` | Completed process or the exact original exception; the owner advances before invocation so it cannot retry |
| 2 | Mac-to-Windows copy | fixed `scp -F`, the exact transaction files and derived Windows destination | size-derived upload timeout; `check=True` | Completed process or exact exception |
| 3 | Windows SHA-256 | `ssh_base` plus the fixed encoded `Get-FileHash` script for every expected basename | size-derived hash timeout; `check=True` | Completed process; legacy policy parses stdout, compares every digest and publishes the unchanged job evidence |
| 4 | server directory claim | fixed nested SSH command and the existing atomic no-clobber guard bytes on stdin | default finite timeout; `check=True` | Completed process or exact exception |
| 5 | Windows-to-server copy | fixed nested `scp`, exact transaction files and `/home/user100/SDL/<project>` | size-derived upload timeout; `check=True` | Completed process or exact exception |
| 6 | qsub once | fixed nested SSH command and byte-identical receipt/qsub shell script on stdin | default finite timeout; `check=False` | Completed process; legacy policy alone classifies submitted/unknown and preserves retry=0 |

There is one `run(...)` statement in `_LegacyRawEffectOwner._invoke`. The
transaction owner is the only effect-plan factory caller, the only raw-owner
factory caller, and the only caller of all six typed step methods. Result and
failure observations preserve the original completed process or exception;
the original catch boundaries, messages, exit codes and reconciliation remain
outside the raw owner.

## Gap matrix and closure decision

| Surface | Legacy policy or raw effect | PR4J action | Remaining gap |
| --- | --- | --- | --- |
| Input capture, staging and immutable file publication | legacy state/materialization policy | unchanged and kept in `_execute_legacy_transaction_once` | none for mechanical extraction |
| Exact reservation and `submission_uncertain` publication | legacy state policy | unchanged; raw owner cannot reserve or mutate state | a future consumer must separately prove its exact reservation transaction |
| Six external runner calls | uniquely reusable raw effect | moved behind one ordered token-gated owner; exact builders, inputs, timeouts and check flags retained | no protected consumer exists |
| Hash parsing and job evidence | legacy outcome/state policy | unchanged at the original inter-step position | future reuse must not reinterpret the observation |
| Resource and approval replay | legacy approval policy | unchanged before qsub | future owner-sealed lifecycle must bind and replay its own approved facts |
| Pre-qsub reconciliation | legacy recovery policy | unchanged outside the raw owner | cannot be generalized without a separate contract |
| qsub classification, receipt and submitted reconciliation | legacy outcome/state policy | unchanged outside the raw owner | a future adapter still needs exact lifecycle and local-state closure |
| `LegacyTransportAdapter.invoke_reserved_once` | fail-closed placeholder | frozen and never called | PR4H/actual adapter remains not PASS |
| PR4D/PR4F/PR4G and historical hashes/fixtures | versioned frozen contracts | no migration, rehash or backfill; PR4J adds a successor fixture only | their independent acceptance and blockers are unchanged |

Closure is limited to mechanical extraction of the fixed raw effect chain.
PR4J adds no protected factory, reads no PR4F handoff, changes no CLI dispatch,
and does not make PR4H or an actual adapter reusable or approved. Offline PASS
does not authorize deployment, transport, PBS, Gaussian, live smoke, merge,
release, retry, cancellation or cleanup.
