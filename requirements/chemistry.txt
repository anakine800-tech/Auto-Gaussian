# Optional dependencies for ChemDraw parsing, depiction, and conformer paths.
# The core offline audit and validation suite uses only the Python standard
# library. This indirection makes even a plain ``pip install -r`` consume the
# reviewed exact lock rather than resolving floating minimum versions.
-r chemistry.lock.txt
